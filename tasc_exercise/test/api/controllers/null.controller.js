var should = require('should');
var request = require('supertest');

describe('controllers', function() {
    describe('none', function() {
        describe('GET /test', function() {
            it('Default placeholder running test suite', function(done) {
                done();
            });
        });
    });
});