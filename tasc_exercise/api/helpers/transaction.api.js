'use strict';
const util = require('util');

function TransactionApi(items) {
    var self = this;
    this.items = items;

    this.save = function() {
    	return new promise();
    }
}

module.exports = TransactionApi;